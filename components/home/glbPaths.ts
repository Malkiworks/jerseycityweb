/** Served from `public/` — add new assets here and preload in `GLBModels.tsx` */
export const GLB = {
  googleLogo: "/google_logo.glb",
  minimalPhone: "/minimal_phone.glb",
  mapPointer: "/map_pointer_3d_icon.glb",
  iconFolder: "/icon_folder.glb",
} as const;
